<Window x:Class="Group5FinalAssignment.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Group5FinalAssignment"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="6*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Name="TxtInput" Grid.Row="1" Grid.Column="1" AcceptsReturn="True" TextWrapping="Wrap" Text="{Binding Input}"></TextBox>
        <TextBlock Name="LblOutput" Grid.Row="2" Grid.Column="1" Text="{Binding Ouput}" TextWrapping="Wrap"></TextBlock>
        <Button Name="BtnExecute" Content="Execute String" FontSize="25" Grid.Row="3" Grid.Column="1" Click="BtnExecute_Click"></Button>
    </Grid>
</Window>
